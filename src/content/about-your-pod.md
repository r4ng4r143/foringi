Pods are balanced using a search algorithm that tries to seat players at similar power levels.

The score is a sum of penalties — 0 means a perfect match. Lower is better. The scale depends on the number of players, so compare scores within the same session rather than across sessions.
